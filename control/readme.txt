Control software for ARCONS data reduction pipeline.  Runs observation data through pipeline and creates image stacks.

To create a new run, add an entry to the dictionary file located at /Scratch/DisplayStack/runList.dict.  In order to add a new target to a run, copy the template located at /Scratch/DisplayStack/RUN_TEMPLATE and replace values with system parameters.
